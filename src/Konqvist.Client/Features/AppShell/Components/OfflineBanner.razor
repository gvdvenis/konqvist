@if (Visible)
{
    <MudAlert Severity="Severity.Warning"
              Dense="true"
              Variant="Variant.Filled"
              Class="app-offline-banner">
        You are offline. Reconnection handling will be added in a later slice.
    </MudAlert>
}

@code {
    [Parameter] public bool Visible { get; set; }
}
